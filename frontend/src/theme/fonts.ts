// IE Design System — Font Family Constants
// Single source of truth for all font-family values across the app

/** Full font stack with Thai fallback (Inter + Noto Sans Thai) */
export const FONT_FAMILY = "'Inter', 'Noto Sans Thai', sans-serif"

/** Latin-only font stack (Inter only, for numbers/labels without Thai) */
export const FONT_FAMILY_LATIN = "'Inter', sans-serif"
